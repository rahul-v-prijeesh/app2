   var s1,s2,c1,c2,r1,r2
   var sry="nr"
   var sr,tr
   var mre
   var i
 function gh() {
 
 }

 function setup()

 {
 //  createCanvas(windowWidth,windowHeight);
 createCanvas(500,500);
 game=new Game
ti=new data()
  game.look()    
  /*FUN
console.error("your computer got hacked")
console.error("getting information...")
console.error("getting information..."+68+""+"%")
alert("this page seems dangerous")
console.error("getting information...100%")
*/
mr=new controller()
    //slider=createSlider(1,tno+567,1)
  // loop();
 
 }

                                                                                                    
 function draw() {
 background(123);
    game.value()
    
    drawSprites()
    ti.tn()
   // noLoop()
    mr.triangle()
   
  // console.warn("bye bye")
  /* loop() ;redraw()*/
    game.find()
     if(calc===false){
    for(i=1;i<array.length;i++){
    if(i<array.length&&i!==0){
      array[0].totalarea=array[0].totalarea+array[i].area
      array[i].area=0
     }  }}
    
 }
 function touchIsDown() {
  
}
